function sayHi(name) {
  return `Hello there ${name}`
}

module.exports = sayHi
// Here is a comment to explain the code above. The function sayHi takes a name as an argument and returns a greeting string that includes the provided name. The function is then exported using module.exports so that it can be used in other files.